package com.authentication.utils;

import com.authentication.validation.EmailValidate;
import com.authentication.validation.ProfileValidation;

public class util {
	public static ProfileValidation sProValidate = new ProfileValidation();
	public static EmailValidate sEmailValid = new EmailValidate();
}
